# 02 — Data Sources

> **זה המסמך החשוב ביותר ברפו.** הוא מכיל ידע דומיין שאי אפשר להסיק מהקוד.
> קרא אותו במלואו לפני כתיבת קוד ingestion.

---

## 1. הרקע החוקי

חוק קידום התחרות בענף המזון, התשע"ד-2014 ותקנות שקיפות מחירים.
כל "קמעונאי גדול" (מחזור > 250 מיליון ₪) חייב לפרסם באתר אינטרנט:

1. קובץ חנויות הרשת
2. לכל חנות בנפרד — המחיר הכולל העדכני של כל מצרך
3. לכל חנות בנפרד — מכלול המבצעים

עדכון מחיר בפרסום עד שעה ממועד העדכון בקופות.
עמוד ריכוז הקישורים: `gov.il` → `cpfta_prices_regulations`.

**החוק מחייב פרסום — הוא לא מחייב פורמט אחיד.** מכאן כל הכאב שלהלן.

---

## 2. סוגי קבצים

| קובץ | תוכן | קצב |
|---|---|---|
| `Stores` | סניפים, כתובות, מזהים | יומי |
| `PricesFull` | snapshot מלא של כל המחירים בסניף | יומי, מוקדם בבוקר |
| `Prices` | דלתות — שינויים תוך־יומיים | כל שעה |
| `PromoFull` | מבצעים מלא | יומי |
| `Promo` | דלתות מבצעים | כל שעה |

**אסטרטגיית קליטה:** `*Full` פעם ביום, דלתות כל שעה.

---

## 3. הרשתות (סופי — 12 רשתות, 13 סקרייפרים)

אומת מול `il-supermarket-scraper` 1.0.8 ב-2026-08-10.
כתובות מלאות ומזהי רשת: [`PHASE0-FINDINGS.md`](PHASE0-FINDINGS.md) §1.

| רשת | Scraper | פורטל | התחברות |
|---|---|---|---|
| שופרסל (כולל רשת BE) | `SHUFERSAL` | עצמאי | ללא |
| רמי לוי | `RAMI_LEVY` | Cerberus (FTP) | שם משתמש |
| קרפור ✚ יינות ביתן ✚ מגה בעיר | `YAYNO_BITAN_AND_CARREFOUR` | PublishPrice | ללא |
| ויקטורי | `VICTORY_NEW_SOURCE` | laibcatalog API | ללא |
| מחסני השוק | `MAHSANI_ASHUK_NEW_SOURCE` | laibcatalog API | ללא |
| אושר עד | `OSHER_AD` | Cerberus (FTP) | שם משתמש |
| מעיין 2000 | `MAAYAN_2000` | Bina | ללא |
| נתיב החסד (כולל ברכל) | `NETIV_HASED` | web — IP ישיר ⚠️ מושבת | ללא |
| שפע ברכת השם | `SHEFA_BARCART_ASHEM` | Bina | ללא |
| זול ובגדול | `ZOL_VEBEGADOL` | Bina | ללא |
| שוק העיר | `SHUK_AHIR` | Bina | ללא |
| קייטי / משנת יוסף | `MESHMAT_YOSEF_1` + `_2` | Cloudflare Worker ✚ Bina | ללא |

**⚠️ `VICTORY` ו-`MAHSANI_ASHUK` אינם fallbacks — הם המקור היחיד.**
השמות הישנים נמחקו מהספרייה; `ScraperFactory.get('VICTORY')` זורק `ValueError`.
זה בדיוק התרחיש שבגללו נכתב ADR-006. ראה `PHASE0-FINDINGS.md` F-1.

**⚠️ `NETIV_HASED` מוחזר כמושבת** — האתר מחזיר HTTP 500 מאז 2026-07-24,
והספרייה מדלגת עליו בשקט. הפייפליין חייב להבחין בין "0 קבצים" ל"דילוג". F-5.

**⚠️ `MESHMAT_YOSEF_1/2` הפוכים ביחס לגרסה קודמת של המסמך:**
`_2` הוא ה-Bina (`ktshivuk.binaprojects.com`), ו-`_1` הוא Cloudflare **Worker**
(`list-files.w5871031-kt.workers.dev`) — לא `chp-kt.pages.dev`. F-4.

**כלל:** הרץ `python scripts/phase0_verify_scrapers.py` בכל שדרוג של הספרייה.
רשתות מתחלפות ברמת gov.il תוך שבועות (קופיקס וקוויק נבלעו ברמי לוי ב-04.08.2026).

### 3.1 בונוס: קרפור = 3 מותגים
`YAYNO_BITAN_AND_CARREFOUR` מכסה את כל קבוצת יינות ביתן —
**קרפור, יינות ביתן, מגה בעיר**. אינטגרציה אחת, שלושה מותגים.

---

## 4. משפחות פורטלים

12 רשתות ≈ **5** דפוסי גישה (המסמך אמר 4 — laibcatalog התווסף). הספרייה
מטפלת בכולם, אבל חשוב להבין למה:

### Cerberus — `url.retail.publishedprices.co.il`
רמי לוי, אושר עד. פורטל משותף לרשתות רבות.
**זה FTP, לא HTTP.** שם המשתמש מקודד בספרייה (`RamiLevi`, `osherad`),
הסיסמה ריקה — ⇒ ייתכן ש`chains.credentials_ref` מיותר כאן. אל תשכפל סוד שאין.

### שופרסל — פורטל עצמאי
`https://prices.shufersal.co.il/`. ללא התחברות. פורמט משלו, עימוד לפי `page`.

### Bina Projects — `http://{chain}.binaprojects.com`
מעיין 2000, שפע ברכת השם, זול ובגדול, שוק העיר, משנת יוסף 2.
הדף בפועל הוא `MainIO_Hok.aspx` (לא `Main.aspx`), **מעל HTTP לא מוצפן**.
**ללא התחברות.** הטבלה ריקה בטעינה ומתמלאת ב־AJAX — רשימת הקבצים
מגיעה כ־JSON מ־endpoint נפרד. אם תצטרך אדפטר משלך: DevTools → Network → XHR.
**אותו דפוס בכל תת־דומיין** — אדפטר אחד מכסה כמה רשתות.

### laibcatalog API — `https://laibcatalog.co.il`
ויקטורי, מחסני השוק. `/webapi/api/getbranches` ו-`/webapi/api/getfiles?edi={chain_id}`.
**אינטגרציה אחת, שתי רשתות** — כמו הבונוס של קרפור ב-§3.1.

### PublishPrice — `https://prices.{infix}.co.il`
קבוצת יינות ביתן (`infix=carrefour`). ללא התחברות.

### web — כתובת ישירה
נתיב החסד (IP חשוף מעל HTTP, כרגע מת) ומשנת יוסף 1 (Cloudflare Worker
שמחזיר JSON). אין כאן דפוס משותף — כל אחד בפני עצמו.

---

## 5. 🔥 כאוס הפורמטים — המלכודות

| מלכודת | פירוט | טיפול |
|---|---|---|
| **קידוד** | חלק מהרשתות ב־UTF-16, לא UTF-8 | זהה BOM. אל תניח. |
| **דחיסה** | רוב gzip, חלק ZIP | sniff לפי magic bytes |
| **גודל** | 5–15MB לקובץ, מאות קבצים בריצה | `lxml.iterparse` בלבד |
| **סכמות** | שמות שדות שונים בין רשתות | מיפוי per-chain בקונפיג |
| **תאריכים** | פורמטים שונים | נרמל ל־ISO 8601 + `Asia/Jerusalem` |
| **מחירים** | לפעמים string עם רווחים / ריק | parse עם fallback ל־`NULL`, **לא 0** |
| **geo-blocking** | חלק מהאתרים חוסמים גישה מחו"ל | הרץ מ־IP ישראלי |

---

## 6. שדות מפתח

### פריט
- `ItemCode` — המזהה. **`ItemType` קובע מה הוא:**
  - `ItemType = 1` → ברקוד גלובלי (EAN-13/8). ניתן להתאמה חוצת־רשתות. ✅
  - `ItemType = 0` → קוד פנימי. שקיל / מותג פרטי. לא ניתן להתאמה ישירה. ⚠️
- `ItemName` — שם בעברית, פורמט חופשי לגמרי
- `ManufacturerName`, `ManufactureCountry`
- `UnitQty`, `Quantity`, `UnitOfMeasure`, `bIsWeighted`
- `ItemPrice`, `UnitOfMeasurePrice`
- `PriceUpdateDate`

### מבצע
שדות מובנים נפוצים: `PromotionId`, `PromotionDescription` (טקסט חופשי!),
`PromotionStartDate/Hour`, `PromotionEndDate/Hour`, `DiscountType`,
`RewardType`, `MinQty`, `DiscountRate`, `DiscountedPrice`, `ClubId`,
`AllowMultipleDiscounts`.

⚠️ **חלק מלוגיקת המבצע קיימת רק בטקסט החופשי של `PromotionDescription`.**
שם השדות המדויק משתנה בין רשתות — **מפה אמפירית ב־Phase 0**, אל תסמוך על
הרשימה הזו כמוחלטת.

---

## 7. שלוש קטגוריות מוצרים — הבנה קריטית

| קטגוריה | ברקוד? | התאמה חוצת־רשתות? | v1? |
|---|---|---|---|
| **מותג לאומי** (תנובה, אסם, שטראוס) | ✅ EAN-13 גלובלי | ✅ טריוויאלית | ✅ |
| **מותג פרטי** | ✅ בד"כ `729…` תקין | ❌ **אין מקבילה זהה** | ⏳ v2 |
| **שקיל / פתוח** (ירקות, בשר, דלפק) | ❌ קוד פנימי | ⚠️ ברמת קטגוריה בלבד | ⏳ v2 |

**התובנה שקל לפספס:** הבעיה במותג פרטי היא **לא** שחסר ברקוד — יש לו ברקוד
GS1 תקין לגמרי. הבעיה היא שהמוצר קיים רק ברשת אחת. אין "אותו קוטג' שופרסל
ירוק" ברמי לוי. שום שיפור באלגוריתם הברקוד לא יפתור את זה — צריך מושג אחר:
**מחלקות שקילות** (v2).

---

## 8. הערכות נפח

| | |
|---|---|
| סניפים בסה"כ (12 רשתות) | ~750–950 |
| פריטים לסניף | ~35,000–50,000 |
| קבצים ביום | ~1,000–2,000 |
| נפח הורדה יומי | ~5–15 GB |

**נפח האחסון קטן בהרבה** בזכות מודל `base + exceptions` — ראה `03-DATA-MODEL.md` §2.
מספר הסניפים משפיע על רוחב פס, לא על אחסון. לכן: **קלוט ארצית**, סנן ברדיוס בשאילתה.

---

## 9. קיצור דרך ל־MVP

קיים dataset ציבורי ב־Kaggle עם נתונים מנורמלים, מתעדכן יומית
(מתוחזק ע"י פרויקט OpenIsraeliSupermarkets).

שווה לבנות עליו את לוגיקת הקטלוג והסל **לפני** שמקימים pipeline משלך —
מקצר את Phase 1 בשבועות. ה־pipeline העצמאי נדרש לפני production בגלל
טריות הנתונים ושליטה בזמינות.
