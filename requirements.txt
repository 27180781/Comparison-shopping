# REQUIRES LINUX OR macOS. Not Windows.
# il-supermarket-scraper imports fcntl (POSIX-only) at module level, so it
# cannot be imported on Windows at all. Use WSL2 there. See PHASE0-FINDINGS F-11.
#
# REQUIRES PYTHON 3.11-3.13. Not 3.14.
# No lxml 5.x release ships a cp314 wheel on any platform, and current distros
# (Ubuntu 26.04) already default to 3.14 - so do not trust the system python3.
# The README pins 3.13 via uv.
#
# Pinned to the versions Phase 0 was verified against. Bump deliberately, and
# re-run `python scripts/phase0_verify_scrapers.py` afterwards: this library
# removes scraper names when a chain changes portal (see PHASE0-FINDINGS F-1).

# ─── Scraping ────────────────────────────────────────
il-supermarket-scraper==1.0.8
lxml==5.4.0

# ─── Database ────────────────────────────────────────
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# ─── Raw storage (Cloudflare R2, S3-compatible) ──────
boto3==1.35.71

# ─── Config ──────────────────────────────────────────
python-dotenv==1.0.1
